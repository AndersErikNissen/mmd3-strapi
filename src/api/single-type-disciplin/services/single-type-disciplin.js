'use strict';

/**
 * single-type-disciplin service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::single-type-disciplin.single-type-disciplin');
